# Generated outputs

Running `process_mining_analysis.py` creates:

- `summary.json` — analysis parameters and top-level event-log statistics
- `temporal_profile.csv` — mean and standard deviation between activity pairs
- `direct_transition_performance.csv` — mean performance for directly-following activities

Generated output files are ignored by Git by default so the repository stays clean.
